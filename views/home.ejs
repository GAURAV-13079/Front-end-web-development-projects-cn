<!DOCTYPE html>
<html lang="en">
<head>
    <title>To Do List</title>
    <link rel="stylesheet" href="https://pro.fontawesome.com/releases/v5.10.0/css/all.css" integrity="sha384-AYmEC3Yw5cVb3ZcuHtOA93w35dYTsvhLPVnYs9eStHfGJvOvKxVfELGroGkvsg+p" crossorigin="anonymous"/>
    <link href="https://fonts.googleapis.com/css2?family=Roboto+Slab:wght@300&display=swap" rel="stylesheet">
    <link href="./css/styles.css" rel="stylesheet" type="text/css">
</head>
<body>
    <div id="main-div">
        <form action="" method="post">
            <div id="heading"><h1>TODO App</h1></div>

            <div class="input-rows" style="width: 100%;">
                <label for="">DISCRIPTION</label>
                <textarea name="" id="" cols="30" rows="2" placeholder="what do you need to do"></textarea>
            </div>
            <div class="input-rows" id="category">
                <label for="">CATEGORY</label>
                <select name="category" class="select">
                    <option value="none">Choose a Category</option>
                    <option value="personal">Personal</option>
                    <option value="work">Work</option>
                    <option value="school">School</option>
                    <option value="cleaning">Cleaning</option>
                    <option value="other">Other</option>
                </select>
            </div>
            <div class="input-rows">
                <label for="">DUE DATE</label>
                <input type="date" class="select" name="due-date">
            </div>

            <div class="buttons">
                <button type="submit" id="add"><i class="fas fa-plus"></i>Add Task</button>
                <button type="submit" style="background-color: rgba(255,0,0,0.8);" id="delete"><i class="far fa-trash-alt"></i>Delete Tasks</button>
            </div>
        </form>
        <div id="task-list">
            <div class="task">
                <input type="checkbox" class="checkbox">
                <% for (i of tasks){%>
                    <span><%= i.name %></span>
                    <p><i class="fas fa-calendar-alt"></i><span><%= i.date %></span></p>
                    <% if (i.type!='none'){ %>
                        <div class="tag"><%= i.type %></div>
                    <% } %>
                <% } %>
            </div>
            <div class="task">
                <input type="checkbox" class="checkbox">
                <span>Why not add a task</span>
                <p><i class="fas fa-calendar-alt"></i><span>August 4, 2021</span></p>
                <div class="tag">Home</div>
            </div>
            
            <!-- <div class="task"></div> -->
            <!-- <div class="task"></div> -->
        </div>
    </div>
</body>
</html>